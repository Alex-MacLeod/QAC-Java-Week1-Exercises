package exercises;

public class EvenlySpaced {
	//public boolean evenlySpaced(int x, int y, int z) {
		/*int a = Math.max(Math.max(x, y), z);
		int b = //give middle number
		int c = Math.min(Math.min(x, y), z);
		
		if (a-b == b-c) {
			return true;
		} else {
			return false;
		}*/
	//}
}
//incomplete